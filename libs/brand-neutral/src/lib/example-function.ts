import { exampleFunction as wlFunction } from '@nx-next-white-label/brand-wl';

export function exampleFunction() {
    return [...wlFunction(), 'This is the result of the NEUTRAL function, extending white-label'];
}